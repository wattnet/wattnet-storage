# wattnet-storage does not read environment variables directly.
# Configuration is injected by the consumer (e.g. wattnet-api) via StorageConfig.
# See config/.env.example in wattnet-api for the full list of variables.
